import React from "react";

export default function ConsolePage() {
  return (
    <main>
      <section style={{ maxWidth: "640px", margin: "40px auto", padding: "0 16px" }}>
        <h1>HX2 Console</h1>
        <p>
          HX2 <code>/console</code> route is online. 
          This is a minimal placeholder page added via Git so we can stop the 404
          and wire in real status widgets later.
        </p>
      </section>
    </main>
  );
}
